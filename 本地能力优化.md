```markdown
# Yupoo 系统级能力扩展与部署优化指令 (Claude Code 专用)

## 核心原则
- **严格限定修改范围**：仅能修改本指令“涉及文件清单”中列出的文件。其他任何未列出的 `.rs`、`.toml`、`.md` 文件均不得改动（包括注释和格式）。
- **成熟库优先**：所有新能力必须使用 Rust 生态中已验证的、下载量高且持续维护的 crate，严禁从零实现通用的底层逻辑（如 HTTP 客户端、子进程管理、浏览器驱动等）。
- **安全内建**：任何涉及系统调用、文件读写、网络请求的操作，必须默认包含超时机制和权限限制，并在代码注释中清晰标注安全注意事项。
- **不破坏现有功能**：现有全部 34 个测试必须保持通过，交互式 REPL、`run`、`demo`、`gui` 入口的行为不得受任何影响。

---

## 目标概述
为 Yupoo（原 plan-executor）补齐三大系统级本地操作能力，同时优化首次桌面启动体验，使其成为一个开箱即用、安全可控的个人 AI 助手：

1. **终端命令执行**：支持执行外部命令，具备超时、输出捕获和基础安全限制。
2. **网络请求能力**：支持 HTTP GET/POST，返回结构化文本。
3. **浏览器自动化**：能够启动并控制本地浏览器（如 Chrome）完成导航、截图、点击、文本提取等操作。
4. **基础安全沙箱**：为文件访问和命令执行提供可配置的白名单与超时保护。
5. **启动体验优化**：生成双击即可运行的脚本，解决 macOS 上“右键打开无反应”的问题。

---

## 涉及文件清单（白名单）
仅允许修改或新增以下文件，其余文件一律不得触碰：

- `Cargo.toml` （仅追加新依赖，保留原有内容不变）
- `src/task.rs` （仅扩展 `Step` 枚举，原有变体必须完全保留）
- `src/agent.rs` （仅增加新步骤的分支处理逻辑，不改动原有分支）
- `src/tools/mod.rs` （新增，作为工具模块入口）
- `src/tools/network.rs` （新增，封装网络请求）
- `src/tools/terminal.rs` （新增，封装终端命令执行与安全限制）
- `src/tools/browser.rs` （新增，封装浏览器自动化）
- `src/safety.rs` （新增，安全管理：白名单、超时、黑名单等）
- `scripts/yupoo.command` （新增，macOS 双击启动脚本）
- `scripts/install.sh` （新增，Linux 快速安装脚本）
- `yupoo-config.example.toml` （新增，安全策略配置模板）
- `tests/integration_tools.rs` （新增，为新工具功能编写集成测试）

**特别注意**：`src/main.rs`、`src/error.rs`、`src/db.rs`、`src/lib.rs`，以及所有现有 `tests/` 目录下的测试文件，均不得修改。

---

## 分阶段执行指令

### 阶段 1：依赖补充
**目标**：在 `Cargo.toml` 中追加新依赖，并确保项目可编译。

**操作要求**：
- 在 `[dependencies]` 末尾追加以下 crate（请使用 crates.io 上的最新稳定版本）：
  - `subprocess` ：高级子进程管理，提供管道、输出捕获和超时能力。
  - `reqwest` ：启用 `json` 和 `rustls-tls` 特性，作为异步 HTTP 客户端。
  - `which` ：用于在系统 PATH 中定位可执行文件（如浏览器路径）。
  - `path-jail` ：轻量路径监狱，用于限制文件访问范围。
- 确保原有所有依赖及其版本声明维持原样，不删不改。
- 追加完成后执行 `cargo check`，必须顺利通过。

### 阶段 2：扩展任务步骤
**目标**：在 `src/task.rs` 的 `Step` 枚举中增加三种新步骤变体，以表达终端执行、网络请求和浏览器操作。

**操作要求**：
- 保留原有 `Think`、`ToolCall`、`WaitForInput`、`Finish` 四个变体，不修改其名称、字段或属性。
- 新增三个变体：
  - `Exec` ：包含 `command`（字符串）、`args`（字符串数组）、`timeout_secs`（可选整数，秒）字段。
  - `HttpRequest` ：包含 `url`（字符串）、`method`（GET 或 POST）、`body`（可选字符串）、`headers`（可选键值映射）字段。
  - `BrowserAction` ：包含 `action`（枚举类型）、`url`（可选字符串）、`selector`（可选字符串）、`timeout_secs`（可选整数）字段。
- 为 `BrowserAction` 创建独立的 `BrowserActionType` 枚举，包含 `Navigate`、`Screenshot`、`Click`、`GetText` 四个值，并使用 `serde(tag)` 和 `rename_all` 确保正确的序列化格式。
- 所有新增类型必须派生 `Debug, Clone, Serialize, Deserialize, PartialEq`。

### 阶段 3：构建安全基础模块
**目标**：新增 `src/safety.rs`，提供一个可配置的安全上下文，为后续所有系统操作提供统一的安全控制点。

**操作要求**：
- 实现 `SafetyContext` 结构体，其内部需包含：
  - 可选的文件访问根路径限制（使用 `path-jail` 库）。
  - 默认命令执行超时时间（`std::time::Duration`）。
  - 禁止执行的命令黑名单（如 `sudo`、`rm`、`mkfs` 等危险命令）。
- 为该结构体实现从配置文件 (`yupoo-config.toml`) 加载的构造方法，若文件不存在则回退到内置的宽松默认值。
- 提供两个核心方法：
  - `validate_command(command: &str) -> Result<(), String>`：检查命令是否在黑名单中，恶意指令直接拒绝。
  - `apply_file_jail(path: &Path) -> Result<PathBuf, Error>`：将给定的用户侧路径转换为受限制的沙箱内路径。
- 所有方法必须在文档注释中说明其安全目的。

### 阶段 4：封装三大执行工具
**目标**：在 `src/tools/` 目录下新增三个文件，分别实现网络请求、终端执行和浏览器自动化，每个操作都需遵循安全基座的要求。

**总体要求**：
- 每个工具模块均提供 `async fn execute(...)` 形式的入口函数，返回 `AgentResult<String>`（字符串为操作结果）。
- 所有可能长时间运行或发起外部调用的逻辑必须内建超时，超时后返回描述性错误。
- 错误均使用你这个 crate 已定义的 `AgentError`，不要引入新的错误枚举。

#### 4.1 网络请求工具 (`src/tools/network.rs`)
**操作要求**：
- 提供 `async fn execute_http_request(...)` 方法，接收 `url`、`method`、`body`、`headers`。
- 使用 `reqwest` 库发送异步请求。
- 强制设置 30 秒超时，超时返回 `AgentError::External("HTTP request timed out")`。
- 响应体限制最大 5MB，防止内存溢出，超出返回错误。
- 返回响应状态码和文本内容（截断后保留前 5000 字符，超出部分用 `...` 替代）。
- 禁止访问 `localhost` 和 `127.0.0.1`，防止 SSRF 攻击，尝试访问时直接返回错误。

#### 4.2 终端命令执行工具 (`src/tools/terminal.rs`)
**操作要求**：
- 提供 `async fn execute_command(...)` 方法，接收命令字符串、参数数组、超时秒数。
- 使用 `subprocess` 库的 `Popen` 进行命令执行，启用管道捕获 stdout/stderr。
- 执行前调用 `SafetyContext::validate_command` 检查命令合法性。
- 若超时参数未提供，则使用 `SafetyContext` 中的默认超时；到达超时时间后强制终止子进程并返回超时错误。
- 将 stdout 和 stderr 合并后返回，最大长度限制为 10000 字符，超出部分进行友好截断提示。
- 命令执行环境变量中应移除可能泄露信息的变量（如 `AWS_SECRET_ACCESS_KEY` 等），仅保留基本 PATH。

#### 4.3 浏览器自动化工具 (`src/tools/browser.rs`)
**操作要求**：
- 提供 `async fn execute_browser_action(...)` 方法，接收动作类型、URL、CSS选择器、超时。
- 由于 Rust 端直接嵌入浏览器驱动会大幅增加二进制体积和编译时间，现阶段采用 **调用系统已安装的 `google-chrome` 或 `chromium` 命令行工具** 的方式来实现截图和无头导航。
- 使用 `which` 库定位系统上的浏览器可执行文件，若找不到则返回描述性错误。
- 根据 `BrowserActionType` 使用 `std::process::Command` 进行不同操作：
  - `Navigate`：启动浏览器打开指定 URL（可提供 `--headless --disable-gpu` 参数），并保存页面源码供后续提取。
  - `Screenshot`：使用浏览器 headless 模式截图，将截图保存到临时目录，返回文件路径作为结果。
  - `Click` 与 `GetText`：初期可返回“暂不支持”的提示，因为这需要更高级的 DOM 交互，未来可升级为 `chromiumoxide` 库。
- 所有浏览器操作都必须强制设置超时（默认 30 秒），超时后杀死浏览器进程。
- 截图文件应保存到系统临时目录，并使用 `SafetyContext` 的路径监狱保证不会写入到工作目录之外的敏感位置。

### 阶段 5：集成到 Agent 执行循环
**目标**：修改 `src/agent.rs`，让 `run_next_step` 方法能处理新增的三种步骤，且不破坏原有的步骤逻辑。

**操作要求**：
- 在 `Agent` 结构体中添加一个 `SafetyContext` 字段，并在 `new` 和 `resume` 方法中初始化。
- 在 `run_next_step` 的 `match` 块中，为 `Step::Exec`、`Step::HttpRequest`、`Step::BrowserAction` 增加对应的处理分支。
- 每个新分支需调用对应工具模块的执行函数，并将返回的字符串结果作为步骤输出。
- 处理完成后必须调用 `advance_and_checkpoint` 记录执行进度（与现有 `Think`、`ToolCall` 分支保持一致）。
- 若工具调用返回错误，**仍应将该错误信息作为步骤输出写入 Checkpoint**（即错误不会导致 Agent 崩溃，而是将错误描述记录为执行结果），以确保断点恢复后用户能审查出错的步骤。
- 不得改动原有 `Think`、`ToolCall`、`WaitForInput`、`Finish` 的任何处理逻辑，仅追加新分支。

### 阶段 6：桌面启动体验优化
**目标**：创建可直接双击运行的脚本，解决在 macOS 上双击 `.app` 无反应的实际问题。

**操作要求**：
- 新增文件 `scripts/yupoo.command`：
  - 一个 bash 脚本，内容为通过绝对路径调用 `yupoo` 二进制文件后进入交互式 REPL，并在执行完后保持终端窗口不关闭（例如使用 `read -p` 等待用户按键）。
  - 赋予执行权限。
- 新增文件 `scripts/install.sh`：
  - 一个 Linux 快速安装脚本，将编译好的 `yupoo` 二进制复制到 `/usr/local/bin`，并验证安装。
  - 包含基本的错误检查和提示信息。
- 在项目根目录新增 `yupoo-config.example.toml`：
  - 提供一个完整的安全配置模板，包含所有键的注释说明，让用户能快速了解如何配置：
    - `allowed_paths`：白名单路径列表
    - `forbidden_commands`：黑名单命令列表
    - `default_timeout_secs`：命令默认超时
    - `browser_path`：浏览器可执行文件路径（可选，自动检测优先）

### 阶段 7：为新功能编写集成测试
**目标**：确保新增工具函数的逻辑正确且安全的约束生效。

**操作要求**：
- 新增文件 `tests/integration_tools.rs`，建立以下测试用例（使用 `#[tokio::test]`）：
  - `test_http_request_success`：对一个公开的测试 API 发起 GET 请求，验证返回非空。
  - `test_http_request_localhost_blocked`：验证对 `http://localhost` 的请求被正确拦截。
  - `test_command_execution`：执行 `echo "hello yupoo"` 并断言输出包含对应字符串。
  - `test_forbidden_command_blocked`：尝试执行 `sudo echo test` 并验证被 `SafetyContext` 拒绝。
  - `test_browser_screenshot`：预先依赖系统安装 chrome，调用截图动作并验证临时目录中生成了 `.png` 文件。
  - `test_command_timeout`：执行 `sleep 10` 并设置 2 秒超时，确认返回超时错误。
- 所有测试不得引入外部网络依赖（除特意用于网络测试的公开地址外），且应具备清理临时文件的操作。
- 运行 `cargo test` 应保证全部通过（包括原有 34 个测试在内）。

---

## 最终验收标准
完成以上所有阶段后，项目必须满足：
1. `cargo check` 和 `cargo test` 全绿，无任何警告或失败。
2. 运行 `yupoo` 进入 REPL 后，能通过输入自然语言文本（被解析为 `Exec` 或 `HttpRequest` 类型步骤）来执行基础终端命令或网络请求。
3. 在 macOS 上双击 `yupoo.command` 能直接打开终端并进入交互界面。
4. 尝试执行 `sudo rm -rf /` 等危险命令时被安全模块拒绝并返回友好提示。
5. 所有新增功能的行为在代码注释和 `yupoo-config.example.toml` 中均有清晰说明。
```

